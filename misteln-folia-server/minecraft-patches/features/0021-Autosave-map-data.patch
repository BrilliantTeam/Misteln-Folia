From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: RICE0707 <master.end.9377@gmail.com>
Date: Tue, 15 Sep 2026 12:20:58 +0800
Subject: [PATCH] Autosave map data


diff --git a/io/papermc/paper/threadedregions/RegionizedServer.java b/io/papermc/paper/threadedregions/RegionizedServer.java
index e1842ab8f20271a8b20ab8462b09a4745a612a9a..08a9564a6dd13d52de08a3080a8a358e2002d848 100644
--- a/io/papermc/paper/threadedregions/RegionizedServer.java
+++ b/io/papermc/paper/threadedregions/RegionizedServer.java
@@ -432,7 +432,20 @@ public final class RegionizedServer {
         world.updateTickData();
 
         world.moonrise$getChunkTaskScheduler().chunkHolderManager.processTicketUpdates(); // required to eventually process ticket updates
+
+        this.autoSaveMaps(world); // Folia - autosave map data
+    }
+
+    // Folia start - autosave map data
+    private void autoSaveMaps(final ServerLevel world) {
+        final int autoSavePeriod = net.minecraft.server.MinecraftServer.getServer().autosavePeriod;
+        final long currTime = System.nanoTime();
+        if (autoSavePeriod > 0 && (currTime - world.lastMapAutoSave) > (java.util.concurrent.TimeUnit.SECONDS.toNanos((long)autoSavePeriod) / 20L)) {
+            world.lastMapAutoSave = currTime;
+            world.getDataStorage().saveMaps();
+        }
     }
+    // Folia end - autosave map data
 
     private void updateRaids(final ServerLevel world) {
         world.getRaids().globalTick();
diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 5e3be75b27ed609be005e8169dc84c125518253e..e0ac07a37c9b542d33288b102302e4b4c0de708a 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -206,6 +206,7 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
     private final StructureManager structureManager;
     private final StructureCheck structureCheck;
     public final boolean tickTime; // Folia - region threading
+    public long lastMapAutoSave = System.nanoTime(); // Folia - autosave map data
     private final RandomSequences randomSequences;
 
     // CraftBukkit start
diff --git a/net/minecraft/world/level/storage/DimensionDataStorage.java b/net/minecraft/world/level/storage/DimensionDataStorage.java
index 225eb4479094771ad6c67a1298ede1c40f9adce9..78f6e541bd5d0913289c35edbdb4c73807b67a8f 100644
--- a/net/minecraft/world/level/storage/DimensionDataStorage.java
+++ b/net/minecraft/world/level/storage/DimensionDataStorage.java
@@ -221,6 +221,38 @@ public class DimensionDataStorage implements AutoCloseable {
         this.scheduleSave().join();
     }
 
+    // Folia start - autosave map data
+    // maps are already safe to save off the owning region, other saved data is not
+    public CompletableFuture<?> saveMaps() {
+        Map<SavedDataType<?>, CompoundTag> map = new Object2ObjectArrayMap<>();
+        RegistryOps<Tag> registryOps = this.registries.createSerializationContext(NbtOps.INSTANCE);
+        synchronized (this.cache) {
+            this.cache.forEach(
+                (savedDataType, optional) -> optional.filter(savedData -> savedData.isDirty()
+                        && (savedData instanceof net.minecraft.world.level.saveddata.maps.MapItemSavedData || savedData instanceof net.minecraft.world.level.saveddata.maps.MapIndex))
+                    .ifPresent(savedData -> {
+                        map.put(savedDataType, this.encodeUnchecked(savedDataType, savedData, registryOps));
+                        savedData.setDirty(false);
+                    })
+            );
+        }
+        if (map.isEmpty()) {
+            return CompletableFuture.completedFuture(null);
+        }
+
+        this.pendingWriteFuture = this.pendingWriteFuture
+            .thenCompose(
+                object -> CompletableFuture.allOf(
+                    map.entrySet()
+                        .stream()
+                        .map(entry -> CompletableFuture.runAsync(() -> this.tryWrite(entry.getKey(), entry.getValue()), Util.DIMENSION_DATA_IO_POOL))
+                        .toArray(CompletableFuture[]::new)
+                )
+            );
+        return this.pendingWriteFuture;
+    }
+    // Folia end - autosave map data
+
     @Override
     public void close() {
         this.saveAndJoin();
