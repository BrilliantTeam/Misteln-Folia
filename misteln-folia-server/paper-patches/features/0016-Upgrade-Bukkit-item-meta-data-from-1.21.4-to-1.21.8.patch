From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: RICE0707 <master.end.9377@gmail.com>
Date: Tue, 15 Sep 2026 22:32:27 +0800
Subject: [PATCH] Upgrade Bukkit item meta data from 1.21.4 to 1.21.8

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaBookSigned.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaBookSigned.java
index eb21c7bf833bbcbc93f30f85af1cdb74f31c63da..e2cfdb1ce6ffa8fd896bcd068a0b1cf23c2487a5 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaBookSigned.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaBookSigned.java
@@ -42,6 +42,7 @@ public class CraftMetaBookSigned extends CraftMetaItem implements BookMeta {
     // Stored as components and serialized as JSON. See SPIGOT-5063, SPIGOT-5350, SPIGOT-3206
     protected List<Component> pages; // null and empty are two different states internally
     protected boolean resolved;
+    private List<String> legacyPagesJson; // Misteln - DFU: json pages as serialized by Bukkit, upgraded in setVersion
     protected int generation;
 
     CraftMetaBookSigned(CraftMetaItem meta) {
@@ -102,9 +103,11 @@ public class CraftMetaBookSigned extends CraftMetaItem implements BookMeta {
         Iterable<?> pages = SerializableMeta.getObject(Iterable.class, map, CraftMetaBookSigned.BOOK_PAGES.BUKKIT, true);
         if (pages != null) {
             this.pages = new ArrayList<>();
+            this.legacyPagesJson = new ArrayList<>(); // Misteln - DFU: keep the json to upgrade it in setVersion
             for (Object page : pages) {
                 if (page instanceof String) {
                     this.internalAddPage(CraftChatMessage.fromJSONOrString((String) page, false, true, CraftMetaBookSigned.MAX_PAGE_LENGTH, false));
+                    this.legacyPagesJson.add((String) page); // Misteln - DFU
                 }
             }
         }
@@ -282,6 +285,23 @@ public class CraftMetaBookSigned extends CraftMetaItem implements BookMeta {
         this.resolved = resolved;
     }
 
+    // Misteln start - DFU: re-read json pages written before the 1.21.5 text component format
+    @Override
+    public void setVersion(int version) {
+        super.setVersion(version);
+        final List<String> legacyPages = this.legacyPagesJson;
+        this.legacyPagesJson = null;
+        if (legacyPages != null && this.pages != null && legacyPages.size() == this.pages.size() && ItemMetaDataUpgrader.needsTextUpgrade(version)) {
+            for (int i = 0; i < legacyPages.size(); i++) {
+                final Component page = ItemMetaDataUpgrader.upgradeText(legacyPages.get(i), version);
+                if (page != null) {
+                    this.pages.set(i, page);
+                }
+            }
+        }
+    }
+    // Misteln end
+
     @Override
     public CraftMetaBookSigned clone() {
         CraftMetaBookSigned meta = (CraftMetaBookSigned) super.clone();
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
index 36a90fa49534bb84cf18899da574bca8bb9a763f..ce3df566e71c891ada5221194069a817d683cdfe 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
@@ -352,6 +352,11 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
     private DataComponentPatch legacyUnhandledPatch;
     private String legacyUnhandledError;
     // Misteln end
+    // Misteln start - DFU: json text as serialized by Bukkit, upgraded in setVersion (the text component format changed in 1.21.5)
+    private @Nullable String legacyDisplayNameJson;
+    private @Nullable String legacyItemNameJson;
+    private @Nullable List<?> legacyLoreJson;
+    // Misteln end
 
     CraftMetaItem(CraftMetaItem meta) {
         if (meta == null) {
@@ -611,10 +616,15 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
     CraftMetaItem(Map<String, Object> map) {
         this.displayName = CraftChatMessage.fromJSONOrString(SerializableMeta.getString(map, CraftMetaItem.NAME.BUKKIT, true), true, false);
         this.itemName = CraftChatMessage.fromJSONOrNull(SerializableMeta.getString(map, CraftMetaItem.ITEM_NAME.BUKKIT, true));
+        // Misteln start - DFU: keep the json to upgrade it in setVersion
+        this.legacyDisplayNameJson = SerializableMeta.getString(map, CraftMetaItem.NAME.BUKKIT, true);
+        this.legacyItemNameJson = SerializableMeta.getString(map, CraftMetaItem.ITEM_NAME.BUKKIT, true);
+        // Misteln end
 
         Iterable<?> lore = SerializableMeta.getObject(Iterable.class, map, CraftMetaItem.LORE.BUKKIT, true);
         if (lore != null) {
             CraftMetaItem.safelyAdd(lore, this.lore = new ArrayList<>(), true);
+            this.legacyLoreJson = com.google.common.collect.Lists.newArrayList(lore); // Misteln - DFU: keep the json to upgrade it in setVersion
         }
 
         Object customModelData = SerializableMeta.getObject(Object.class, map, CraftMetaItem.CUSTOM_MODEL_DATA.BUKKIT, true);
@@ -668,6 +678,12 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
             }
         }
 
+        // Misteln start - DFU: hide-tool-tip was replaced by the tooltip display in 1.21.5
+        if (SerializableMeta.getBoolean(map, "hide-tool-tip")) {
+            this.setHideTooltip(true);
+        }
+        // Misteln end
+
         String tooltipStyle = SerializableMeta.getString(map, CraftMetaItem.TOOLTIP_STYLE.BUKKIT, true);
         if (tooltipStyle != null) {
             this.setTooltipStyle(NamespacedKey.fromString(tooltipStyle));
@@ -752,6 +768,11 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
         CraftJukeboxComponent jukeboxPlayable = SerializableMeta.getObject(CraftJukeboxComponent.class, map, CraftMetaItem.JUKEBOX_PLAYABLE.BUKKIT, true);
         if (jukeboxPlayable != null) {
             this.setJukeboxPlayable(jukeboxPlayable);
+            // Misteln start - DFU: show-in-tooltip was replaced by the tooltip display in 1.21.5
+            if (jukeboxPlayable.legacyHiddenInTooltip) {
+                this.hideComponentInTooltip(DataComponents.JUKEBOX_PLAYABLE);
+            }
+            // Misteln end
         }
 
         Integer damage = SerializableMeta.getObject(Integer.class, map, CraftMetaItem.DAMAGE.BUKKIT, true);
@@ -821,6 +842,15 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
         }
     }
 
+    // Misteln start - DFU: show_in_tooltip of a component became a hidden component of the tooltip display in 1.21.5
+    private void hideComponentInTooltip(DataComponentType<?> type) {
+        if (this.tooltipDisplay == null) {
+            this.tooltipDisplay = new TooltipDisplay(false, new ReferenceLinkedOpenHashSet<>());
+        }
+        this.tooltipDisplay.hiddenComponents().add(type);
+    }
+    // Misteln end
+
     // Misteln start - DFU: moved out of the map constructor so upgraded components can be applied again
     private void applyUnhandled(DataComponentPatch unhandledPatch) {
         CraftMetaItem.getOrEmpty(unhandledPatch, CraftMetaItem.CAN_PLACE_ON).ifPresent(data -> {
@@ -2441,6 +2471,31 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
     @Override
     public void setVersion(int version) {
         this.version = version;
+        // Misteln start - DFU: re-read json text written before the 1.21.5 text component format
+        if (ItemMetaDataUpgrader.needsTextUpgrade(version)) {
+            final Component upgradedDisplayName = ItemMetaDataUpgrader.upgradeText(this.legacyDisplayNameJson, version);
+            if (upgradedDisplayName != null) {
+                this.displayName = upgradedDisplayName;
+            }
+            final Component upgradedItemName = ItemMetaDataUpgrader.upgradeText(this.legacyItemNameJson, version);
+            if (upgradedItemName != null) {
+                this.itemName = upgradedItemName;
+            }
+            if (this.legacyLoreJson != null && this.lore != null && this.legacyLoreJson.size() == this.lore.size()) {
+                for (int i = 0; i < this.legacyLoreJson.size(); i++) {
+                    if (this.legacyLoreJson.get(i) instanceof String line) {
+                        final Component upgradedLine = ItemMetaDataUpgrader.upgradeText(line, version);
+                        if (upgradedLine != null) {
+                            this.lore.set(i, upgradedLine);
+                        }
+                    }
+                }
+            }
+        }
+        this.legacyDisplayNameJson = null;
+        this.legacyItemNameJson = null;
+        this.legacyLoreJson = null;
+        // Misteln end
         // Misteln start - DFU: upgrade the unhandled components if they were written by an older version
         final CompoundTag legacyTag = this.legacyUnhandledTag;
         final DataComponentPatch legacyPatch = this.legacyUnhandledPatch;
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgrader.java b/src/main/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgrader.java
index 68c25dcb80f733fd1c061550bbf55c920a9fa7a0..d4b67ca67f3f181341da7f687534b46e12b0f664 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgrader.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgrader.java
@@ -43,4 +43,37 @@ final class ItemMetaDataUpgrader {
         }
         return converted;
     }
+
+    // text components were json with camelCase click/hover events and string booleans before 1.21.5 (see V4290, V4291, V4292)
+    static boolean needsTextUpgrade(final int version) {
+        return version < ca.spottedleaf.dataconverter.minecraft.versions.V4290.VERSION;
+    }
+
+    // json text as written by Bukkit (display name, lore, book pages), null if it is not json or needs no upgrade
+    static net.minecraft.network.chat.@Nullable Component upgradeText(final @Nullable String json, final int fromVersion) {
+        if (json == null || !needsTextUpgrade(fromVersion)) {
+            return null;
+        }
+        try {
+            final com.google.gson.JsonElement element = com.google.gson.JsonParser.parseString(json);
+            if (!element.isJsonObject() && !element.isJsonArray()) {
+                return null; // plain (legacy formatted) text is read the same by every version
+            }
+        } catch (final RuntimeException ex) {
+            return null;
+        }
+        final Object converted = MCDataConverter.convert(MCTypeRegistry.TEXT_COMPONENT, json, fromVersion, CraftMagicNumbers.INSTANCE.getDataVersion());
+        final net.minecraft.nbt.Tag tag = switch (converted) {
+            case ca.spottedleaf.dataconverter.types.nbt.NBTMapType map -> map.getTag();
+            case ca.spottedleaf.dataconverter.types.nbt.NBTListType list -> list.getTag();
+            case String string -> net.minecraft.nbt.StringTag.valueOf(string);
+            case null, default -> null;
+        };
+        if (tag == null) {
+            return null;
+        }
+        return net.minecraft.network.chat.ComponentSerialization.CODEC
+            .parse(org.bukkit.craftbukkit.CraftRegistry.getMinecraftRegistry().createSerializationContext(net.minecraft.nbt.NbtOps.INSTANCE), tag)
+            .result().orElse(null);
+    }
 }
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/components/CraftJukeboxComponent.java b/src/main/java/org/bukkit/craftbukkit/inventory/components/CraftJukeboxComponent.java
index eff1cad2c360f5c548c0390375ae090cfb12c047..2b9bec33101856cb0b3ac0f15f44a3f5cb853e32 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/components/CraftJukeboxComponent.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/components/CraftJukeboxComponent.java
@@ -23,6 +23,7 @@ import org.bukkit.inventory.meta.components.JukeboxPlayableComponent;
 public final class CraftJukeboxComponent implements JukeboxPlayableComponent {
 
     private JukeboxPlayable handle;
+    public boolean legacyHiddenInTooltip; // Misteln - DFU: show-in-tooltip: false before 1.21.5, see CraftMetaItem
 
     public CraftJukeboxComponent(JukeboxPlayable jukebox) {
         this.handle = jukebox;
@@ -35,6 +36,7 @@ public final class CraftJukeboxComponent implements JukeboxPlayableComponent {
     public CraftJukeboxComponent(Map<String, Object> map) {
         String song = SerializableMeta.getObject(String.class, map, "song", false);
 
+        this.legacyHiddenInTooltip = Boolean.FALSE.equals(SerializableMeta.getObject(Boolean.class, map, "show-in-tooltip", true)); // Misteln - DFU
         this.handle = new JukeboxPlayable(new EitherHolder<>(ResourceKey.create(Registries.JUKEBOX_SONG, ResourceLocation.parse(song))));
     }
 
diff --git a/src/test/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgradeTest.java b/src/test/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgradeTest.java
index 3a835dbe631451287ff45d601fdb8af311e90c0b..d4b0a28011da98ab25bc293fd15dd7b0dfbf3dbf 100644
--- a/src/test/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgradeTest.java
+++ b/src/test/java/org/bukkit/craftbukkit/inventory/ItemMetaDataUpgradeTest.java
@@ -27,11 +27,12 @@ import org.bukkit.support.environment.AllFeatures;
 import org.bukkit.tag.DamageTypeTags;
 import org.junit.jupiter.api.Test;
 
-// Misteln - DFU: items serialized by 1.21.1 (data version 3955) must still load
+// Misteln - DFU: items serialized by 1.21.1 (data version 3955) and 1.21.4 (4189) must still load
 @AllFeatures
 public class ItemMetaDataUpgradeTest {
 
     private static final int V1_21_1 = 3955;
+    private static final int V1_21_4 = 4189;
 
     static String base64(CompoundTag tag) throws IOException {
         ByteArrayOutputStream buf = new ByteArrayOutputStream();
@@ -246,4 +247,117 @@ public class ItemMetaDataUpgradeTest {
         assertEquals(40.0, map.getBaseValue(Attributes.MAX_HEALTH));
         assertEquals(0.5, map.getBaseValue(Attributes.MOVEMENT_SPEED));
     }
+
+    @Test
+    public void testLegacyJsonText() {
+        ItemStack stack = load(V1_21_4, "STICK", ""
+            + "    meta-type: UNSPECIFIC\n"
+            + "    display-name: '{\"text\":\"shop\",\"bold\":\"true\",\"clickEvent\":{\"action\":\"open_url\",\"value\":\"https://example.com\"},\"hoverEvent\":{\"action\":\"show_text\",\"contents\":\"hi\"}}'\n"
+            + "    lore:\n"
+            + "    - '{\"text\":\"run\",\"clickEvent\":{\"action\":\"run_command\",\"value\":\"/spawn\"}}'\n"
+            + "    - plain line\n"
+            + "    hide-tool-tip: true\n");
+
+        net.minecraft.world.item.ItemStack nms = CraftItemStack.asNMSCopy(stack);
+        net.minecraft.network.chat.Component name = nms.get(DataComponents.CUSTOM_NAME);
+        assertEquals("shop", name.getString());
+        assertTrue(name.getStyle().isBold(), "string boolean -> boolean");
+        assertInstanceOf(net.minecraft.network.chat.ClickEvent.OpenUrl.class, name.getStyle().getClickEvent());
+        assertInstanceOf(net.minecraft.network.chat.HoverEvent.ShowText.class, name.getStyle().getHoverEvent());
+
+        java.util.List<net.minecraft.network.chat.Component> lore = nms.get(DataComponents.LORE).lines();
+        assertEquals("/spawn", ((net.minecraft.network.chat.ClickEvent.RunCommand) lore.get(0).getStyle().getClickEvent()).command());
+        assertEquals("plain line", lore.get(1).getString());
+
+        assertTrue(stack.getItemMeta().isHideTooltip(), "hide-tool-tip -> tooltip display");
+    }
+
+    @Test
+    public void testLegacyBookPages() {
+        ItemStack book = load(V1_21_4, "WRITTEN_BOOK", ""
+            + "    meta-type: BOOK_SIGNED\n"
+            + "    title: t\n"
+            + "    author: a\n"
+            + "    pages:\n"
+            + "    - '{\"text\":\"next\",\"clickEvent\":{\"action\":\"change_page\",\"value\":\"2\"}}'\n");
+
+        net.minecraft.network.chat.Component page = CraftItemStack.asNMSCopy(book).get(DataComponents.WRITTEN_BOOK_CONTENT).pages().getFirst().raw();
+        assertEquals(2, ((net.minecraft.network.chat.ClickEvent.ChangePage) page.getStyle().getClickEvent()).page());
+    }
+
+    @Test
+    public void testLegacyTooltipComponents() throws IOException {
+        ItemStack disc = load(V1_21_4, "MUSIC_DISC_CAT", ""
+            + "    meta-type: UNSPECIFIC\n"
+            + "    jukebox-playable:\n"
+            + "      ==: JukeboxPlayable\n"
+            + "      song: minecraft:cat\n"
+            + "      show-in-tooltip: false\n");
+        assertTrue(CraftItemStack.asNMSCopy(disc).get(DataComponents.TOOLTIP_DISPLAY).hiddenComponents().contains(DataComponents.JUKEBOX_PLAYABLE));
+
+        CompoundTag nested = new CompoundTag();
+        nested.putString("id", "minecraft:stone");
+        nested.putInt("count", 1);
+        CompoundTag nestedComponents = new CompoundTag();
+        nestedComponents.putString("minecraft:custom_name", "{\"text\":\"inner\",\"clickEvent\":{\"action\":\"open_url\",\"value\":\"https://example.com\"}}");
+        CompoundTag unbreakable = new CompoundTag();
+        unbreakable.putBoolean("show_in_tooltip", false);
+        nestedComponents.put("minecraft:unbreakable", unbreakable);
+        nested.put("components", nestedComponents);
+        CompoundTag slot = new CompoundTag();
+        slot.putInt("slot", 0);
+        slot.put("item", nested);
+        ListTag container = new ListTag();
+        container.add(slot);
+        CompoundTag unhandled = new CompoundTag();
+        unhandled.put("minecraft:container", container);
+
+        ItemStack stack = load(V1_21_4, "STICK", ""
+            + "    meta-type: UNSPECIFIC\n"
+            + "    unhandled: " + base64(unhandled) + "\n");
+
+        net.minecraft.world.item.ItemStack inner = CraftItemStack.asNMSCopy(stack).get(DataComponents.CONTAINER).copyOne();
+        assertInstanceOf(net.minecraft.network.chat.ClickEvent.OpenUrl.class, inner.get(DataComponents.CUSTOM_NAME).getStyle().getClickEvent());
+        assertTrue(inner.get(DataComponents.TOOLTIP_DISPLAY).hiddenComponents().contains(DataComponents.UNBREAKABLE), "show_in_tooltip -> tooltip display");
+    }
+
+    @Test
+    public void testLegacyEntityEquipment() throws IOException {
+        CompoundTag sword = new CompoundTag();
+        sword.putString("id", "minecraft:iron_sword");
+        sword.putInt("count", 1);
+        ListTag handItems = new ListTag();
+        handItems.add(sword);
+        handItems.add(new CompoundTag());
+        CompoundTag entity = new CompoundTag();
+        entity.putString("id", "minecraft:zombie");
+        entity.put("HandItems", handItems);
+        CompoundTag internal = new CompoundTag();
+        internal.put("entity-tag", entity);
+
+        ItemStack egg = load(V1_21_4, "ZOMBIE_SPAWN_EGG", ""
+            + "    meta-type: SPAWN_EGG\n"
+            + "    internal: " + base64(internal) + "\n");
+
+        CompoundTag upgraded = CraftItemStack.asNMSCopy(egg).get(DataComponents.ENTITY_DATA).copyTag();
+        assertEquals("minecraft:iron_sword", upgraded.getCompoundOrEmpty("equipment").getCompoundOrEmpty("mainhand").getString("id").orElseThrow(), upgraded.toString());
+    }
+
+    @Test
+    public void testCurrentTextUntouched() {
+        ItemStack stack = new ItemStack(Material.WRITTEN_BOOK);
+        org.bukkit.inventory.meta.BookMeta meta = (org.bukkit.inventory.meta.BookMeta) stack.getItemMeta();
+        meta.displayName(net.kyori.adventure.text.Component.text("{\"text\":\"looks like json\"}").clickEvent(net.kyori.adventure.text.event.ClickEvent.openUrl("https://example.com")));
+        meta.lore(java.util.List.of(net.kyori.adventure.text.Component.text("[1, 2]")));
+        meta.setTitle("t");
+        meta.setAuthor("a");
+        meta.addPages(net.kyori.adventure.text.Component.text("page").clickEvent(net.kyori.adventure.text.event.ClickEvent.changePage(1)));
+        stack.setItemMeta(meta);
+
+        YamlConfiguration config = new YamlConfiguration();
+        config.set("item", stack);
+        ItemStack loaded = YamlConfiguration.loadConfiguration(new StringReader(config.saveToString())).getItemStack("item");
+
+        assertEquals(stack, loaded);
+    }
 }
