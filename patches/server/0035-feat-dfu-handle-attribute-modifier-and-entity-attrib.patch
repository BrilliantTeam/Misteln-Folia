From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: RICE0707 <master.end.9377@gmail.com>
Date: Tue, 2 Jun 2026 23:46:14 +0800
Subject: [PATCH] feat(dfu): handle attribute modifier and entity attribute
 1.20.5/6 to 1.21 upgrade

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

diff --git a/src/main/java/net/minecraft/world/entity/ai/attributes/AttributeMap.java b/src/main/java/net/minecraft/world/entity/ai/attributes/AttributeMap.java
index 69992ebc999ea3ff9e47e4e049bcc514c01150ca..4253494e59dc3f48c7250149315c3757d4569900 100644
--- a/src/main/java/net/minecraft/world/entity/ai/attributes/AttributeMap.java
+++ b/src/main/java/net/minecraft/world/entity/ai/attributes/AttributeMap.java
@@ -123,9 +123,57 @@ public class AttributeMap {
         return listTag;
     }
 
+    // Misteln start - entity attribute upgrade (1.20.5/6 -> 1.21)
+    private static CompoundTag upgradeAttributeIfNeeded(CompoundTag tag) {
+        if (!tag.contains("id") && tag.contains("Name")) {
+            CompoundTag up = tag.copy();
+            up.putString("id", up.getString("Name"));
+            up.remove("Name");
+
+            if (up.contains("Base")) {
+                up.putDouble("base", up.getDouble("Base"));
+                up.remove("Base");
+            }
+
+            if (up.contains("Modifiers", 9)) {
+                ListTag oldMods = up.getList("Modifiers", 10);
+                ListTag newMods = new ListTag();
+                for (int j = 0; j < oldMods.size(); j++) {
+                    CompoundTag mod = oldMods.getCompound(j).copy();
+                    if (mod.contains("UUID", 11)) {
+                        try {
+                            java.util.UUID uuid = net.minecraft.nbt.NbtUtils.loadUUID(mod.get("UUID"));
+                            mod.putString("id", "minecraft:" + uuid.toString());
+                        } catch (Exception ignored) {}
+                        mod.remove("UUID");
+                    } else if (mod.contains("Name")) {
+                        mod.putString("id", "minecraft:" + mod.getString("Name"));
+                    }
+                    mod.remove("Name");
+                    if (mod.contains("Amount") && !mod.contains("amount")) {
+                        mod.putDouble("amount", mod.getDouble("Amount"));
+                        mod.remove("Amount");
+                    }
+                    if (mod.contains("Operation", 3) && !mod.contains("operation")) {
+                        String[] ops = {"add_value", "add_multiplied_base", "add_multiplied_total"};
+                        int op = mod.getInt("Operation");
+                        mod.putString("operation", op >= 0 && op < ops.length ? ops[op] : "add_value");
+                        mod.remove("Operation");
+                    }
+                    newMods.add(mod);
+                }
+                up.put("modifiers", newMods);
+                up.remove("Modifiers");
+            }
+            return up;
+        }
+        return tag;
+    }
+    // Misteln end
+
     public void load(ListTag nbt) {
         for (int i = 0; i < nbt.size(); i++) {
-            CompoundTag compoundTag = nbt.getCompound(i);
+            CompoundTag compoundTag = upgradeAttributeIfNeeded(nbt.getCompound(i)); // Misteln
             String string = compoundTag.getString("id");
             ResourceLocation resourceLocation = ResourceLocation.tryParse(string);
             if (resourceLocation != null) {
diff --git a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
index 60ec9b228518de0521b12f3f4ef26cb30260315a..758fc5db7440d3e9f4316fa41b6a8facbda11812 100644
--- a/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
+++ b/src/main/java/org/bukkit/craftbukkit/inventory/CraftMetaItem.java
@@ -740,6 +740,21 @@ class CraftMetaItem implements ItemMeta, Damageable, Repairable, BlockDataMeta {
                 // entry is not an actual NBTTagCompound. getCompound returns empty NBTTagCompound in that case
                 continue;
             }
+            // Misteln start - attribute modifier id upgrade (1.20.5/6 -> 1.21)
+            if (!entry.contains("id") && (entry.contains("uuid", CraftMagicNumbers.NBT.TAG_INT_ARRAY) || entry.contains("name", CraftMagicNumbers.NBT.TAG_STRING))) {
+                entry = entry.copy();
+                if (entry.contains("uuid", CraftMagicNumbers.NBT.TAG_INT_ARRAY)) {
+                    try {
+                        java.util.UUID uuid = net.minecraft.nbt.NbtUtils.loadUUID(entry.get("uuid"));
+                        entry.putString("id", "minecraft:" + uuid.toString());
+                    } catch (Exception ignored) {}
+                    entry.remove("uuid");
+                } else {
+                    entry.putString("id", "minecraft:" + entry.getString("name"));
+                }
+                entry.remove("name");
+            }
+            // Misteln end
             net.minecraft.world.entity.ai.attributes.AttributeModifier nmsModifier = net.minecraft.world.entity.ai.attributes.AttributeModifier.load(entry);
             if (nmsModifier == null) {
                 continue;
